function cube(num) {
    return Math.pow(num, 3);
};
alert(cube(6))

function sum(num1, num2, num3) {
    return num1 + num2 + num3;
}
var num = sum(6, 7, 8);
// alert(num);

// 全局
// 局部（函数）
// alert具有阻塞（暂停）代码的功能